#!/usr/bin/python3
def multiple_returns(sentence):
    pass


def multiple_returns(sentence):
    length = len(sentence)
    first = None if sentence == None else sentence[0]
    return (length, first)
