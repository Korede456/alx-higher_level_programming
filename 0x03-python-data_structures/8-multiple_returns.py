#!/usr/bin/python3
def multiple_returns(sentence):
    pass


def multiple_returns(sentence):
    length = len(sentence)
    first = ""
    if sentence = "" or sentence = None:
        first = ""
    else:
        first = sentence[0]
    return (length, first)
